# TortoiseGit操作ガイド

TortoiseGitは、Windowsエクスプローラーに統合されるGitクライアントです。右クリックメニューから操作できるため、直感的に使用できます。

---

## 初期設定

### 1. TortoiseGitのダウンロードとインストール

- 公式サイト（https://tortoisegit.org/）からダウンロードし、インストールします
- **重要**: Git for Windowsが事前にインストールされている必要があります
- 日本語ランゲージパックも別途ダウンロード可能です

### 2. 初期設定

1. デスクトップやフォルダを右クリック
2. 「TortoiseGit」→「Settings」を選択
3. 「Git」→「Config」を選択
4. 以下の情報を設定します:
   - **Name（名前）**: あなたの名前
   - **Email（メールアドレス）**: あなたのメールアドレス
   - これらの情報はコミット履歴に記録されます
5. 「OK」をクリックして設定を保存

**補足**: 日本語ランゲージパックをインストールした場合、「Settings」→「General」→「Language」で日本語を選択できます。

---

## 主要な操作

### クローン

リモートリポジトリの完全なコピーを自分のコンピュータに作成します。

1. エクスプローラーで保存先のフォルダを開く
   - 新しいフォルダを作成する必要はありません（自動で作成されます）
2. 右クリック→「Git Clone...」を選択
3. 「URL」にリポジトリのURLを入力
   - GitHubなどから取得したURLを貼り付けます
4. 「Directory」で保存先を確認
   - 必要に応じて変更可能
5. 「OK」をクリック

**補足**: 
- クローン中は進捗ダイアログが表示されます
- 完了すると、指定したフォルダにリポジトリが作成されます

---

### ブランチの作成

新しい機能や修正のための独立した作業環境を作ります。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Create Branch...」を選択
3. 「Branch」にブランチ名を入力
   - 作業内容がわかりやすい名前を付けることが推奨されます
4. 「Base On」で作成元となるブランチまたはコミットを選択
   - デフォルトは「HEAD」（現在のブランチの最新コミット）
5. オプションを選択:
   - 「Switch to new branch」: 作成したブランチにすぐに切り替える（通常はオン推奨）
   - 「Track」: リモートブランチを追跡する場合にオン
6. 「OK」をクリック

---

### 変更のコミット

編集した内容を履歴として記録します。

1. リポジトリフォルダ内で右クリック
2. 「Git Commit -> "ブランチ名"...」を選択
3. コミットダイアログが表示されます:
   - 上部: コミットメッセージ入力欄
   - 下部: 変更されたファイルの一覧
4. コミットに含めたいファイルにチェックを入れる
   - 「すべて」ボタンですべてのファイルを選択可能
   - 「なし」ボタンですべてのチェックを外せます
5. 上部のメッセージ欄にコミットメッセージを入力
   - 1行目: 変更の概要（50文字以内推奨）
   - 2行目: 空行（オプション）
   - 3行目以降: 詳細な説明（必要に応じて）
6. 「Commit」ボタンをクリック

**補足**: 
- ファイル名をダブルクリックすると、変更内容の差分を確認できます
- 「Commit & Push」ボタンで、コミットとプッシュを同時に実行できます

---

### プッシュ

ローカルで行ったコミットをリモートリポジトリに送信し、チームメンバーと共有します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Push...」を選択
3. プッシュ設定を確認:
   - **Local**: プッシュするローカルブランチ
   - **Remote**: プッシュ先のリモート（通常は「origin」）
   - **Remote Branch**: プッシュ先のリモートブランチ
4. オプションを選択（必要に応じて）:
   - 「Push all branches」: すべてのブランチをプッシュ
   - 「Push all tags」: すべてのタグをプッシュ
   - 「Force」: 強制プッシュ（注意が必要）
5. 「OK」をクリック

**補足**: 初めてプッシュするブランチの場合、リモートにそのブランチを作成します。

---

### プル

リモートリポジトリの最新の変更を取得し、ローカルに統合します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Pull...」を選択
3. プル設定を確認:
   - **Remote**: プル元のリモート（通常は「origin」）
   - **Remote Branch**: プル元のリモートブランチ
4. オプションを選択（必要に応じて）:
   - 「Rebase」: マージコミットを作らずにリベースで統合
   - 「FFonly」: Fast-forwardマージのみ許可
5. 「OK」をクリック

**補足**: 作業を開始する前に、必ずプルを実行して最新の状態にすることが推奨されます。

---

### フェッチ

リモートの最新情報を取得しますが、自動的には統合しません。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Fetch...」を選択
3. フェッチ元のリモートを確認
   - 通常は「origin」を選択
4. オプションを選択（必要に応じて）:
   - 「All Remotes」: すべてのリモートからフェッチ
   - 「Prune」: リモートで削除されたブランチをローカルからも削除
5. 「OK」をクリック

**補足**: フェッチ後、「TortoiseGit」→「Show log」でリモートの状態を確認し、必要に応じてマージを行います。

---

### マージ

異なるブランチの変更を統合します。

1. マージ先のブランチにチェックアウト
   - 右クリック→「TortoiseGit」→「Switch/Checkout...」でブランチを切り替え
2. リポジトリフォルダ内で右クリック
3. 「TortoiseGit」→「Merge...」を選択
4. マージ元を選択:
   - **Branch**: ブランチ名を指定
   - **Tag**: タグ名を指定
   - **Commit**: 特定のコミットを指定
5. オプションを選択（必要に応じて）:
   - 「No Fast Forward」: 常にマージコミットを作成
   - 「Squash」: マージ元の複数コミットを1つにまとめる
6. 「OK」をクリック

**競合が発生した場合**:
1. 競合が発生すると、該当ファイルが赤いアイコンで表示されます
2. 競合ファイルを右クリック→「TortoiseGit」→「Edit conflicts」を選択
3. TortoiseGitMergeが起動します（次のセクションで詳しく説明）
4. 競合を解決後、ファイルを保存してマージツールを閉じます
5. リポジトリフォルダで右クリック→「Git Commit」を選択
6. コミットメッセージが自動生成されているので、そのままコミット

---

## TortoiseGitMergeを使った競合解決

競合が発生すると、TortoiseGitMergeという専用ツールで解決できます。

### 画面構成

- **左側（Theirs）**: マージ元（相手）の変更
- **右側（Mine）**: 現在のブランチ（自分）の変更
- **下部（Merged）**: 結果ペイン（最終的な内容）

### 競合の解決手順

1. TortoiseGitMergeが起動すると、競合箇所が色付きで表示されます
2. 各競合箇所を確認しながら、以下のいずれかを選択:
   - 左側の変更を採用: 競合箇所で右クリック→「Use text block from 'theirs'」
   - 右側の変更を採用: 競合箇所で右クリック→「Use text block from 'mine'」
   - 両方を採用: 競合箇所で右クリック→「Use both text blocks」
   - 手動で編集: 下部の結果ペインで直接編集
3. すべての競合を解決したら、ファイルを保存
4. マージツールを閉じる
5. 競合が解決済みとしてマークされます

**補足**: 
- 複数のファイルに競合がある場合、1つずつ順番に解決していきます
- 「Mark as resolved」をクリックして、競合解決済みとマークできます

---

## ブランチの切り替え

作業するブランチを変更します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Switch/Checkout...」を選択
3. 切り替え先のブランチを選択
4. 「OK」をクリック

**補足**: 
- 未コミットの変更がある場合は、警告が表示されます
- 「Force」オプションで、変更を破棄して強制的に切り替えることも可能です（注意が必要）

---

## 履歴の確認

過去のコミット履歴を確認します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Show log」を選択
3. ログビューアーが表示されます:
   - 上部: コミットの一覧（グラフ表示あり）
   - 下部: 選択したコミットの詳細情報
4. コミットをクリックすると、以下の情報が表示されます:
   - コミットメッセージ
   - 作成者と日時
   - 変更されたファイル一覧
5. ファイルをダブルクリックすると、差分を確認できます

**便利な機能**:
- 上部の検索ボックスで、コミットメッセージやファイル名を検索できます
- 「All Branches」オプションで、すべてのブランチの履歴を表示できます

---

## よく使う機能

### タグの作成

特定のコミットにタグ（マーカー）を付けます。リリースバージョンのマーキングなどに使用します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Create Tag...」を選択
3. タグ名を入力（例: v1.0.0）
4. タグを付けるコミットを選択
   - デフォルトは「HEAD」（現在のコミット）
5. 必要に応じてメッセージを入力
6. 「OK」をクリック

**補足**: タグをリモートにプッシュするには、「Push all tags」オプションを使用します。

---

### ブランチの削除

不要になったブランチを削除します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Switch/Checkout...」を選択
3. ブランチ一覧から、削除したいブランチを右クリック
4. 「Delete」を選択
5. 確認ダイアログで「Yes」をクリック

**注意**: 
- マージ済みのブランチのみ削除することを推奨します
- 未マージのブランチを削除しようとすると、警告が表示されます

---

### リビジョングラフ

ブランチの分岐とマージの様子を視覚的に確認します。

1. リポジトリフォルダ内で右クリック
2. 「TortoiseGit」→「Revision Graph」を選択
3. グラフビューアーが表示されます
4. ブランチの構造を視覚的に確認できます

**補足**: コミットをクリックすると、詳細情報を確認できます。

---

## アイコンオーバーレイ

TortoiseGitは、エクスプローラーのアイコンにステータスを表示します。

- **緑のチェック**: 変更なし（クリーン）
- **赤い感嘆符**: 変更あり（未コミット）
- **黄色の感嘆符**: 競合あり
- **グレーのプラス**: 追加されたファイル
- **グレーのマイナス**: 削除されたファイル

**補足**: アイコンが更新されない場合は、F5キーでエクスプローラーを更新してください。

---

## トラブルシューティング

### プッシュできない

**原因**: リモートの方が新しい変更を持っている可能性があります。

**解決方法**:
1. プルを実行して、リモートの変更を取得
2. 競合がある場合は解決
3. 再度プッシュを実行

---

### 間違ったコミットをした

**解決方法（コミット前の場合）**:
- ファイルを右クリック→「TortoiseGit」→「Revert」で、変更を取り消せます

**解決方法（コミット後、プッシュ前の場合）**:
1. 右クリック→「TortoiseGit」→「Show log」
2. 取り消したいコミットを右クリック
3. 「Reset "ブランチ名" to this...」を選択
4. リセットタイプを選択:
   - 「Soft」: コミットを取り消し、変更は保持（ステージングエリアに残る）
   - 「Mixed」: コミットを取り消し、変更は保持（未ステージング状態）
   - 「Hard」: コミットと変更の両方を完全に削除（注意が必要）

**注意**: プッシュ後のコミットを取り消すことは避けるべきです。他のメンバーに影響を与える可能性があります。

---

### アイコンオーバーレイが表示されない

**原因**: 他のシェル拡張と競合している可能性があります。

**解決方法**:
1. 右クリック→「TortoiseGit」→「Settings」
2. 「Icon Overlays」を選択
3. 「Status cache」を「Shell」に変更
4. パソコンを再起動

---

## まとめ

TortoiseGitは、エクスプローラーに統合されているため、ファイル管理と同じ感覚でGit操作を行えます。基本的な操作に慣れたら、Blame（各行の編集者確認）、リベース、パッチ作成などの高度な機能も活用できます。
