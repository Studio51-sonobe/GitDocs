# SourceTree操作ガイド

SourceTreeは、Atlassian社が提供する無料のGitクライアントです。Windows、macOSで利用できます。

---

## 初期設定

### 1. SourceTreeのダウンロードとインストール

- 公式サイト（https://www.sourcetreeapp.com/）からダウンロードし、インストールします
- Atlassianアカウントでのログインが必要です（無料で作成できます）

### 2. Gitの設定

初回起動時に以下の設定を行います：

1. Git for Windowsのパスを設定します
   - 通常は自動で検出されます
2. ユーザー名とメールアドレスを設定します
   - これらの情報はコミット履歴に記録されます

---

## 主要な操作

### クローン

リモートリポジトリの完全なコピーを自分のコンピュータに作成します。

1. 上部の「Clone」ボタンをクリック
2. 「Source Path / URL」にリポジトリのURLを入力
   - GitHubなどから取得したURLを貼り付けます
3. 「Destination Path」に保存先を指定
   - デフォルトではユーザーフォルダ配下に保存されます
4. 「Name」でリポジトリの表示名を確認（変更可能）
5. 「クローン」ボタンをクリック

**補足**: クローン完了後、自動的にそのリポジトリが開かれます。

---

### ブランチの作成

新しい機能や修正のための独立した作業環境を作ります。

1. 上部の「ブランチ」ボタンをクリック
2. 新しいブランチ名を入力
   - 作業内容がわかりやすい名前を付けることが推奨されます
3. 作成元となるブランチまたはコミットを選択
   - 通常は現在のブランチから作成します
4. 「ブランチを作成」ボタンをクリック

**オプション**:
- 「作成後に新しいブランチをチェックアウト」: 作成したブランチにすぐに切り替えます（通常はオン推奨）

---

### 変更のコミット

編集した内容を履歴として記録します。

1. ファイルを編集すると、「ファイルステータス」タブに変更が表示されます
2. コミットしたいファイルを選択し、「ステージへインデックス」をクリック
   - 複数のファイルを選択してまとめてステージできます
   - 「すべてインデックスに追加」ボタンで、すべての変更をステージできます
3. 下部のコミットメッセージ欄にメッセージを入力
   - 1行目: 変更の概要（50文字以内推奨）
   - 2行目: 空行（オプション）
   - 3行目以降: 詳細な説明（必要に応じて）
4. 「コミット」ボタンをクリック

**補足**: 
- ステージングエリアは、コミットに含めたい変更を選択するための場所です
- ファイルの一部だけをコミットしたい場合は、ファイルを右クリック→「ハンクを選択...」で部分的にステージできます

---

### プッシュ

ローカルで行ったコミットをリモートリポジトリに送信し、チームメンバーと共有します。

1. 上部の「プッシュ」ボタンをクリック
2. プッシュしたいブランチを選択
   - 複数のブランチを同時にプッシュすることも可能です
3. プッシュ先のリモートを確認
   - 通常は「origin」が表示されます
4. 「プッシュ」ボタンをクリック

**補足**: 
- 「プッシュ後にタグを送信」オプションで、タグも一緒にプッシュできます
- 初めてプッシュするブランチの場合、リモートにそのブランチを作成します

---

### プル

リモートリポジトリの最新の変更を取得し、ローカルに統合します。

1. 上部の「プル」ボタンをクリック
2. プル元のリモートブランチを確認
3. 以下のオプションを選択（必要に応じて）:
   - 「リモートブランチからプルする際に現在のブランチをリベース」: マージコミットを作らずに変更を統合したい場合にオン
4. 「OK」をクリック

**補足**: 作業を開始する前に、必ずプルを実行して最新の状態にすることが推奨されます。

---

### フェッチ

リモートの最新情報を取得しますが、自動的には統合しません。

1. 上部の「フェッチ」ボタンをクリック
2. フェッチ元のリモートを確認
   - 通常は「origin」を選択
3. 「OK」をクリック

**補足**: フェッチ後、左サイドバーの「リモート」配下でリモートの状態を確認できます。変更内容を確認してから、必要に応じてプルまたはマージを行います。

---

### マージ

異なるブランチの変更を統合します。

1. マージ先のブランチにチェックアウト
   - 左サイドバーのブランチをダブルクリックして切り替え
2. 左サイドバーのブランチ一覧から、マージしたいブランチを右クリック
3. 「現在のブランチへマージ」を選択
4. マージオプションを選択:
   - 「マージコミットを作成（--no-ff）」: 常にマージコミットを作成
   - 「Fast Forwardが可能な場合はコミットを作成しない」: 履歴が一直線の場合はマージコミットを作らない
5. 「OK」をクリック

**競合が発生した場合**:
1. 「競合解決」ダイアログが表示されます
2. 競合しているファイルを選択
3. 「競合解決アクション」から以下のいずれかを選択:
   - 「外部マージツールで解決」: 設定されたマージツールを起動
   - 「マイナルのものを使用」: 自分の変更を優先
   - 「相手のものを使用」: マージ元の変更を優先
4. すべての競合を解決したら、「OK」をクリック
5. コミットして、マージを完了します

---

## ブランチの切り替え

作業するブランチを変更します。

1. 左サイドバーの「ブランチ」から、切り替えたいブランチをダブルクリック
2. 未コミットの変更がある場合は、以下のいずれかを選択:
   - 「変更を破棄」: 変更を削除（注意が必要）
   - 「キャンセル」: ブランチ切り替えを中止し、変更をコミットしてから切り替える

---

## 履歴の確認

過去のコミット履歴を確認します。

1. 「History」タブを選択
2. コミットの一覧が時系列で表示されます
3. コミットをクリックすると、以下の情報が表示されます:
   - コミットメッセージ
   - 作成者と日時
   - 変更されたファイル一覧
   - ファイルごとの差分
4. 右上の検索ボックスで、コミットメッセージやファイル名を検索できます

**補足**: 
- グラフ表示でブランチの分岐とマージの様子を視覚的に確認できます
- 複数のブランチを選択して、ブランチ間の違いを比較できます

---

## よく使う機能

### タグの作成

特定のコミットにタグ（マーカー）を付けます。リリースバージョンのマーキングなどに使用します。

1. 履歴表示で、タグを付けたいコミットを右クリック
2. 「タグ」を選択
3. タグ名を入力（例: v1.0.0）
4. 必要に応じてメッセージを入力
5. 「タグを追加」をクリック

---

### ブランチの削除

不要になったブランチを削除します。

1. 左サイドバーの「ブランチ」から、削除したいブランチを右クリック
2. 「ブランチを削除」を選択
3. 確認ダイアログで「OK」をクリック

**注意**: 
- マージ済みのブランチのみ削除することを推奨します
- 未マージのブランチを削除しようとすると、警告が表示されます

---

### リモートブランチの追跡

リモートに存在するブランチをローカルで作業するための設定を行います。

1. 左サイドバーの「リモート」→「origin」を展開
2. 追跡したいリモートブランチをダブルクリック
3. 自動的にローカルブランチが作成され、リモートブランチを追跡するように設定されます

---

## トラブルシューティング

### プッシュできない

**原因**: リモートの方が新しい変更を持っている可能性があります。

**解決方法**:
1. フェッチまたはプルを実行して、リモートの変更を取得
2. 競合がある場合は解決
3. 再度プッシュを実行

---

### 間違ったコミットをした

**解決方法（コミット前の場合）**:
- 「ファイルステータス」タブで、ファイルを右クリック→「破棄」

**解決方法（コミット後、プッシュ前の場合）**:
1. 履歴表示で、取り消したいコミットを右クリック
2. 「コミットを元に戻す」を選択
   - 「ソフトリセット」: コミットを取り消し、変更は保持（再編集可能）
   - 「ハードリセット」: コミットと変更の両方を完全に削除（注意が必要）

**注意**: プッシュ後のコミットを取り消すことは避けるべきです。他のメンバーに影響を与える可能性があります。

---

### 競合解決ツールの設定

SourceTreeには標準の競合解決ツールが付属していますが、外部ツールを使用することもできます。

1. 「ツール」→「オプション」を選択
2. 「Diff」タブを選択
3. 「外部Diffツール」と「マージツール」を設定
   - 推奨: Beyond Compare、P4Merge、KDiff3など

---

## まとめ

SourceTreeは機能が豊富で、Gitの操作を視覚的に行えるツールです。基本的な操作に慣れたら、インタラクティブリベース、サブモジュール管理などの高度な機能も活用できます。
