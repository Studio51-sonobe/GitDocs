# 環境構築ガイド

このドキュメントでは、Gitを使い始めるための環境構築手順を説明します。

---

## Windowsにおけるgitのセットアップ

Windowsで Git を使用するには、Git for Windows をインストールする必要があります。

### インストール手順

#### 1. Git for Windowsのダウンロード

公式サイト（https://git-scm.com/download/win）から最新版のインストーラーをダウンロードします。

#### 2. インストーラーの実行

ダウンロードしたインストーラーを実行します。基本的にはデフォルト設定のまま「Next」を押して進めて問題ありません。

#### 3. 重要な設定項目

インストール中に表示される以下の設定項目に注意してください：

**エディタの選択**
- コミットメッセージを入力するエディタを選択します
- Visual Studio Code、Vim、Notepad++などから選択可能
- 推奨: Visual Studio Code（使い慣れたエディタがあればそれを選択）

**PATH環境変数**
- 「Git from the command line and also from 3rd-party software」を選択することを推奨
- この設定により、コマンドプロンプトやPowerShellからもGitを使用できます

**改行コードの扱い**
- 「Checkout Windows-style, commit Unix-style line endings」が推奨されます
- Windowsでファイルを編集する際はCRLF、リポジトリにはLFで保存されます
- チーム開発で異なるOSを使用している場合に、改行コードの違いによる問題を防ぎます

**ターミナルエミュレーター**
- 「Use MinTTY (the default terminal of MSYS2)」が推奨されます
- より高機能なターミナル環境を使用できます

#### 4. インストールの完了

インストールが完了したら、コマンドプロンプトまたはPowerShellを開き、以下のコマンドでインストールを確認します：

```
git --version
```

バージョン番号が表示されれば、インストール成功です。

#### 5. 初期設定

Gitを使用する前に、ユーザー名とメールアドレスを設定する必要があります。これらの情報はコミット履歴に記録されます。

コマンドプロンプトまたはPowerShellで以下のコマンドを実行します：

```
git config --global user.name "あなたの名前"
git config --global user.email "your.email@example.com"
```

**補足**:
- 名前は本名でもニックネームでも構いません
- メールアドレスは、GitHubなどのサービスを使用する場合、そのサービスに登録しているものと同じにすることを推奨します

設定を確認するには：

```
git config --global --list
```

---

## Git-LFSのセットアップ

Git-LFS（Git Large File Storage）は、大きなファイルを効率的に扱うためのGitの拡張機能です。画像、動画、3Dモデルなどの大容量ファイルを管理する場合に使用します。

### インストール手順

#### 1. Git-LFSのダウンロード

公式サイト（https://git-lfs.github.com/）から最新版のインストーラーをダウンロードします。

**注意**: Git for Windowsの最新版には、Git-LFSが含まれている場合があります。まず、以下のコマンドで確認してください：

```
git lfs version
```

バージョン番号が表示されれば、すでにインストールされています。

#### 2. インストーラーの実行

Git-LFSがインストールされていない場合、ダウンロードしたインストーラーを実行します。

#### 3. Git-LFSの初期化

コマンドプロンプトまたはPowerShellで以下のコマンドを実行します：

```
git lfs install
```

このコマンドは1回だけ実行すれば、その後のすべてのリポジトリで有効になります。

成功すると、以下のようなメッセージが表示されます：
```
Git LFS initialized.
```

#### 4. 追跡するファイルの指定

Git-LFSで管理したい大きなファイルの種類を指定します。リポジトリのルートディレクトリで以下のコマンドを実行します：

```
git lfs track "*.psd"
git lfs track "*.mp4"
git lfs track "*.zip"
```

**よく使用されるファイルタイプの例**:
- 画像ファイル: `*.psd`, `*.ai`, `*.png` (大きなもの)
- 動画ファイル: `*.mp4`, `*.mov`, `*.avi`
- 音声ファイル: `*.wav`, `*.mp3`
- 3Dモデル: `*.fbx`, `*.obj`, `*.blend`
- アーカイブ: `*.zip`, `*.rar`, `*.7z`
- 実行ファイル: `*.exe`, `*.dll`

#### 5. .gitattributesファイルの確認

上記のコマンドを実行すると、`.gitattributes`ファイルが自動的に作成または更新されます。このファイルには、Git-LFSで追跡するファイルの設定が保存されます。

`.gitattributes`ファイルの例：
```
*.psd filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
```

このファイルは、リポジトリに含めてコミットする必要があります：

```
git add .gitattributes
git commit -m "Add Git LFS tracking"
```

#### 6. 追跡状況の確認

現在追跡されているファイルタイプを確認するには：

```
git lfs track
```

#### 7. Git-LFSで管理されているファイルの確認

すでにGit-LFSで管理されているファイルを確認するには：

```
git lfs ls-files
```

---

## Git-LFSの使用上の注意点

### リモートリポジトリでの対応

Git-LFSを使用する場合、リモートリポジトリ（GitHub、GitLab、Bitbucketなど）がGit-LFSに対応している必要があります。

- **GitHub**: 無料プランでも対応（ストレージとデータ転送に制限あり）
- **GitLab**: 対応
- **Bitbucket**: 対応

### ストレージ容量の確認

Git-LFSには、ストレージ容量や月間データ転送量に制限がある場合があります。大容量ファイルを頻繁に扱う場合は、プランのアップグレードが必要になることがあります。

### 既存のファイルをGit-LFSに移行

すでにリポジトリにコミットされている大きなファイルをGit-LFSで管理したい場合は、`git lfs migrate`コマンドを使用します（高度な操作のため、詳細はGit-LFSのドキュメントを参照してください）。

---

## トラブルシューティング

### Git-LFSがインストールされているか確認できない

以下のコマンドでパスを確認します：

```
where git-lfs
```

パスが表示されない場合は、再インストールが必要です。

### Git-LFSの初期化に失敗する

管理者権限でコマンドプロンプトを開き、再度`git lfs install`を実行してください。

### 大きなファイルのプッシュに失敗する

Git-LFSが正しく設定されていない可能性があります。以下を確認してください：

1. `.gitattributes`ファイルが存在し、正しく設定されているか
2. Git-LFSが初期化されているか（`git lfs install`の実行）
3. リモートリポジトリがGit-LFSに対応しているか

---

## まとめ

- Git for Windowsをインストールし、ユーザー名とメールアドレスを設定することで、Gitの基本的な使用が可能になります
- 大きなファイルを扱う場合は、Git-LFSをインストールし、追跡するファイルタイプを設定します
- 各GUIツール（Fork、SourceTree、TortoiseGit）の使い方については、それぞれの専用ガイドを参照してください

環境構築が完了したら、実際にリポジトリをクローンして、開発を始めることができます。
