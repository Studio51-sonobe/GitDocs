# Fork操作ガイド

Forkは、直感的で視覚的に優れたGitクライアントです。Windows、macOS、Linuxで利用できます。

---

## 初期設定

### 1. Forkのダウンロードとインストール

- 公式サイト（https://git-fork.com/）からダウンロードし、インストールします

### 2. リポジトリを開く

- 「File」→「Open Repository」から既存のローカルリポジトリを開く、または
- 「File」→「Clone」でリモートリポジトリをクローンします

---

## 主要な操作

### クローン

リモートリポジトリの完全なコピーを自分のコンピュータに作成します。

1. 「File」→「Clone」を選択
2. リポジトリのURLを入力
3. 保存先のフォルダを選択
4. 「Clone」ボタンをクリック

**補足**: クローン完了後、自動的にそのリポジトリが開かれます。

---

### ブランチの作成

新しい機能や修正のための独立した作業環境を作ります。

1. 左サイドバーの「Branches」を右クリック
2. 「New Branch」を選択
3. ブランチ名を入力し、作成元となるブランチを選択
4. 「Create Branch」をクリック

**補足**: ブランチ名は、作業内容がわかりやすい名前を付けることが推奨されます（例: feature/login-page、bugfix/header-alignment）。

---

### 変更のコミット

編集した内容を履歴として記録します。

1. ファイルを編集すると、左下の「Changes」タブに変更されたファイルが表示されます
2. コミットに含めたいファイルにチェックを入れます
   - すべてのファイルを選択する場合は、上部の「Stage All」をクリック
3. 下部のテキストボックスにコミットメッセージを入力
   - 1行目: 変更の概要（50文字以内推奨）
   - 2行目: 空行
   - 3行目以降: 詳細な説明（必要に応じて）
4. 「Commit」ボタンをクリック

**補足**: コミットメッセージは、後から履歴を見たときに何を変更したかが分かるように、明確に書くことが重要です。

---

### プッシュ

ローカルで行ったコミットをリモートリポジトリに送信し、チームメンバーと共有します。

1. 上部ツールバーの「Push」ボタンをクリック
2. プッシュ先のリモートとブランチを確認
   - 通常は「origin」というリモート名が表示されます
3. 「Push」をクリック

**補足**: 初めてプッシュするブランチの場合、リモートにそのブランチを作成するかどうか確認されます。

---

### プル

リモートリポジトリの最新の変更を取得し、ローカルに統合します。

1. 上部ツールバーの「Pull」ボタンをクリック
2. プル元のリモートとブランチを確認
3. 「Pull」をクリック

**補足**: 作業を開始する前に、必ずプルを実行して最新の状態にすることが推奨されます。

---

### フェッチ

リモートの最新情報を取得しますが、自動的には統合しません。

1. 上部ツールバーの「Fetch」ボタンをクリック
2. フェッチ元のリモートを確認
3. 「Fetch」をクリック

**補足**: フェッチ後、左サイドバーの「Remotes」配下でリモートの状態を確認できます。その後、必要に応じて手動でマージを行います。

---

### マージ

異なるブランチの変更を統合します。

1. マージ先のブランチにチェックアウト（ブランチをダブルクリック）
2. マージしたいブランチを右クリック
3. 「Merge into current branch」を選択
4. 競合がある場合は、競合解決画面が表示されます

**競合の解決方法**:
1. 競合が発生したファイルが一覧表示されます
2. 各ファイルをクリックして、競合箇所を確認
3. 左側（現在のブランチ）、右側（マージ元のブランチ）を確認しながら、採用する変更を選択
4. すべての競合を解決したら、「Mark as Resolved」をクリック
5. コミットして、マージを完了します

---

## ブランチの切り替え

作業するブランチを変更します。

1. 左サイドバーの「Branches」から、切り替えたいブランチをダブルクリック
2. 未コミットの変更がある場合は、警告が表示されます
   - 変更をコミットしてから切り替えることを推奨します

---

## 履歴の確認

過去のコミット履歴を確認します。

1. 中央の履歴表示エリアに、コミットの一覧が時系列で表示されます
2. コミットをクリックすると、そのコミットで変更されたファイルと差分が表示されます
3. 上部の検索ボックスで、特定のコミットメッセージやファイル名を検索できます

**補足**: ブランチ間の違いを比較したい場合は、2つのブランチを選択して「Compare」機能を使用します。

---

## よく使う機能

### ブランチの削除

不要になったブランチを削除します。

1. 左サイドバーの「Branches」から、削除したいブランチを右クリック
2. 「Delete」を選択
3. 確認ダイアログで「Delete」をクリック

**注意**: マージ済みのブランチのみ削除することを推奨します。未マージのブランチを削除すると、その変更が失われます。

---

## トラブルシューティング

### プッシュできない

**原因**: リモートの方が新しい変更を持っている可能性があります。

**解決方法**:
1. プルを実行して、リモートの変更を取得
2. 競合がある場合は解決
3. 再度プッシュを実行

### 間違ったコミットをした

**解決方法（コミット前の場合）**:
- 変更を取り消すには、ファイルを右クリック→「Discard Changes」

**解決方法（コミット後、プッシュ前の場合）**:
- 最新のコミットを右クリック→「Undo Commit」で、コミットを取り消せます（変更は保持されます）

**注意**: プッシュ後のコミットを取り消すことは避けるべきです。他のメンバーに影響を与える可能性があります。

---

## まとめ

Forkは視覚的にわかりやすく、Gitの操作を直感的に行えるツールです。基本的な操作に慣れたら、タグの作成、リベース、チェリーピックなどの高度な機能も試してみてください。
